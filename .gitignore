# Secrets — never commit API tokens
.env
*.env
!.env.example

# Python / uv
.venv/
__pycache__/
*.pyc
.ruff_cache/
.pytest_cache/
.ipynb_checkpoints/

# Data and model artifacts (regenerated)
data/raw/
data/processed/
models/
*.parquet
*.csv.gz

# Raw GFW API pulls (CC BY-NC; regenerate with a token, don't redistribute in bulk)
data/sar/gfw_*.geojson

# Keep placeholders
!**/.gitkeep

# OS / editor / tooling
.DS_Store
.idea/
.vscode/
node_modules/
.claude/launch.json
